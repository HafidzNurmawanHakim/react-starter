import React from "react";
import SidePanel from "../Components/SidePanel";

const Dashboard = () => {
	return (
		<div className="w-screen h-screen bg-light overflow-hidden">
			<SidePanel />
		</div>
	);
};

export default Dashboard;
